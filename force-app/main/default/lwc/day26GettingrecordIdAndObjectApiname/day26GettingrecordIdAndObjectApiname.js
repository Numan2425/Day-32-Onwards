import { LightningElement,api } from 'lwc';

export default class Day26GettingrecordIdAndObjectApiname extends LightningElement {
    @api recordId; // property name is reserved
    @api objectApiName; // property name is reserved
}